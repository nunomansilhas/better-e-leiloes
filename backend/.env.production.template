# ===========================================
# E-Leilões Dashboard - PRODUCTION CONFIG
# ===========================================
# cPanel: eleiloes.mansilhas.pt
#
# INSTRUCTIONS:
# 1. Copy this to .env in the backend folder
# 2. Fill in the API_SECRET_KEY (generate one below)
# 3. Verify database credentials
# ===========================================

# Security - GENERATE A NEW KEY!
# Run: python -c "import secrets; print(secrets.token_hex(32))"
API_SECRET_KEY=GENERATE_NEW_KEY_HERE

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60

# Database (cPanel MySQL)
# Password: wcDR%QY0BGYt (the % is encoded as %25)
DATABASE_URL=mysql+aiomysql://mansilh1_nuno:wcDR%25QY0BGYt@localhost/mansilh1_eleiloes

# CORS - Production domain
ALLOWED_ORIGINS=https://eleiloes.mansilhas.pt

# Redis (comment out if not available on cPanel)
# REDIS_URL=redis://localhost:6379

# Scraping Configuration
SCRAPE_DELAY=1.0
CONCURRENT_REQUESTS=2
USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36
